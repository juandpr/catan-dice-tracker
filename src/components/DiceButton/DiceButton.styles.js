import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  diceButton: {
    alignItems: 'center',
    justifyContent: 'center',
    height: '100%',
    width: '100%'
  },
  diceButtonText: {
    fontSize: 20,
    color: '#0066CC',  
  }
});
